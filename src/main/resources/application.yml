spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:lfVbdEARTZVIxOKhxqjMFOthbLymEDBP}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:TXlTdXBlclNlY3JldEtleUZvclNhbjNhQmFja2VuZDIwMjZTdHJvbmdLZXk=}
    access-token-expiration-minutes: ${JWT_EXPIRATION_MINUTES:120}
